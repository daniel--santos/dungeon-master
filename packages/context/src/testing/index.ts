/**
 * Utilitários **só para testes** de `@dungeon-master/context`, publicados no
 * subpath `@dungeon-master/context/testing`: o store em memória que
 * implementa as portas sem banco.
 */
export * from "./memory-store.js";
