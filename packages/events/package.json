{
  "name": "@dungeon-master/events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Transporte SSE, drain por cursor e ponte de NOTIFY do PostgreSQL.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "26.5.0",
    "typescript": "6.0.3",
    "vitest": "5.0.0"
  }
}
