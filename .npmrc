# O Node mínimo declarado em engines é obrigatório, não uma sugestão.
engine-strict=true

# Dependências de workspace são sempre resolvidas pelo link local.
link-workspace-packages=true
prefer-workspace-packages=true

# Reprodutibilidade: nada de resolver versões novas sem atualizar o lockfile.
resolution-mode=highest
